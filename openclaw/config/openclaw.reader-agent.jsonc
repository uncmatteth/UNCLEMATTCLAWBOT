// Reader agent template (reads untrusted content, no dangerous tools)
{
  "agent": {
    "name": "reader",
    "tools": {
      "deny": ["group:runtime", "group:browser", "group:ui", "group:fs"]
    }
  }
}
